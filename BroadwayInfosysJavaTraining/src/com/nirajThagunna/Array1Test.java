package com.nirajThagunna;

import java.io.IOException;

public class Array1Test extends Array {

    public static void main(String[] args) throws IOException {
        int MAX = 5;
//        printArray(readArray(MAX));
        // finding sum of elements if an array
//        System.out.println("Sum is : " + doSum(readArray(MAX)));
//        System.out.println("Minimum value is : " + minValue(readArray(MAX)));
        System.out.println("Maximum value is : " + maxValue(readArray(MAX)));
    }
}
